# =============================================================================
# PRODUCTION ENVIRONMENT VARIABLES - EXAMPLE
# =============================================================================
# IMPORTANT: This is an example file. Copy to .env.production and fill in real values
# NEVER commit .env.production or any file with real secrets to version control!
#
# For Kubernetes deployment, these values should be stored in Kubernetes Secrets
# and ConfigMaps, NOT in .env files.
#
# Create secrets with:
# kubectl create secret generic science-portal-secrets \
#   --from-literal=auth-secret=$(openssl rand -base64 32) \
#   --from-literal=oidc-client-secret=<your-oidc-secret>
# =============================================================================

# =============================================================================
# Server-Side API Configuration (NOT exposed to browser)
# =============================================================================
SERVICE_STORAGE_API=https://ws-uv.canfar.net/arc/nodes/home/
LOGIN_API=https://ws-cadc.canfar.net/ac
SKAHA_API=https://ws-uv.canfar.net/skaha
SRC_SKAHA_API=https://src.canfar.net/skaha
SRC_CAVERN_API=https://src.canfar.net/cavern/nodes/home/
AC_API=https://ws-uv.canfar.net/ac
PASSWORD_RESET_URL=https://www.cadc-ccda.hia-iha.nrc-cnrc.gc.ca/access/passwordResetRequest
REGISTRATION_URL=https://www.cadc-ccda.hia-iha.nrc-cnrc.gc.ca/access/control/proxy
API_TIMEOUT=30000

# =============================================================================
# Client-Side Configuration (exposed to browser via NEXT_PUBLIC_ prefix)
# =============================================================================
NEXT_PUBLIC_LOGIN_API=https://ws-cadc.canfar.net/ac
NEXT_PUBLIC_SKAHA_API=https://ws-uv.canfar.net/skaha
NEXT_PUBLIC_SRC_SKAHA_API=https://src.canfar.net/skaha
NEXT_PUBLIC_SRC_CAVERN_API=https://src.canfar.net/cavern/nodes/home/
NEXT_PUBLIC_API_TIMEOUT=30000
NEXT_PUBLIC_ENABLE_QUERY_DEVTOOLS=false

# =============================================================================
# Authentication Configuration
# =============================================================================
# Set to 'true' for CANFAR authentication, 'false' for OIDC (e.g., SKA IAM)
NEXT_USE_CANFAR=true
NEXT_PUBLIC_USE_CANFAR=true

# NextAuth Secret - CRITICAL: Generate a secure random string
# Generate with: openssl rand -base64 32
AUTH_SECRET=REPLACE_WITH_SECURE_RANDOM_STRING

# NextAuth Configuration
AUTH_TRUST_HOST=true
NEXTAUTH_URL=https://your-production-domain.com

# =============================================================================
# OIDC Configuration (for production)
# =============================================================================
NEXT_OIDC_URI=https://your-oidc-provider.com/
NEXT_OIDC_CLIENT_ID=your-client-id
NEXT_OIDC_CLIENT_SECRET=REPLACE_WITH_YOUR_CLIENT_SECRET
NEXT_OIDC_CALLBACK_URI=https://your-production-domain.com/science-portal
NEXT_OIDC_REDIRECT_URI=https://your-production-domain.com/api/auth/callback/oidc
NEXT_OIDC_SCOPE=openid profile offline_access

# Public OIDC Configuration
NEXT_PUBLIC_OIDC_URI=https://your-oidc-provider.com/
NEXT_PUBLIC_OIDC_CLIENT_ID=your-client-id
NEXT_PUBLIC_OIDC_CALLBACK_URI=https://your-production-domain.com/science-portal
NEXT_PUBLIC_OIDC_REDIRECT_URI=https://your-production-domain.com/api/auth/callback/oidc
NEXT_PUBLIC_OIDC_SCOPE=openid profile offline_access

# =============================================================================
# CADC Proxy Configuration (for user registration)
# =============================================================================
# IMPORTANT: Values must be quoted to preserve # characters
CADC_PROXY_RESOURCE_ID="ivo://cadc.nrc.ca/gms"
CADC_PROXY_STANDARD_ID="ivo://ivoa.net/std/UMS#reqs-0.1"
CADC_PROXY_AUTH_TYPE="anon"
CADC_PROXY_INTERFACE_TYPE_ID="http://www.ivoa.net/xml/VODataService/v1.1#ParamHTTP"
